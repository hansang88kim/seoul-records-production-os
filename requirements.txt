streamlit>=1.35.0
pydantic>=2.0.0
python-dotenv>=1.0.0
pillow>=10.0.0
mutagen>=1.47.0
jsonschema>=4.22.0
pytest>=8.0.0
pytest-cov>=5.0.0
rich>=13.0.0
click>=8.1.0
requests>=2.31.0

# v0.8.3: YouTube Data API (real private upload)
google-api-python-client>=2.0.0
google-auth>=2.0.0
google-auth-oauthlib>=1.0.0
google-auth-httplib2>=0.2.0

# v0.9.2: Telegram remote control bot
python-telegram-bot>=21.0

# Real thumbnail image generation (Gemini / Nano Banana). Optional: only needed
# when 'use real images' is on. Set GEMINI_API_KEY (https://aistudio.google.com/apikey).
google-genai>=1.0.0

# v1.0.0-alpha.20: bundled FFmpeg via imageio-ffmpeg (no system install needed)
imageio-ffmpeg>=0.5.1
